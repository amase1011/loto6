services:
  - type: web
    name: loto6-analyzer
    runtime: node
    plan: free
    buildCommand: echo "No build step"
    startCommand: node server.js
    autoDeploy: true
    healthCheckPath: /api/health
